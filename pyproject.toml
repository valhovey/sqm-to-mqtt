[project]
name = "sqm-to-mqtt"
version = "0.1.0"
description = "Script to publish SQM data over MQTT"
readme = "readme.md"
requires-python = ">=3.11"
dependencies = [
    "paho-mqtt==2.1.0",
    "pyserial==3.5",
    "skyfield==1.54"
]
